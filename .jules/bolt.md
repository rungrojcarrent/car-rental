
## 2026-04-18 - Array find vs filter[0]
**Learning:** Using `.find()` is faster and more expressive than `.filter()[0]` for retrieving a single element.
**Action:** Use `.find()` in future iterations to improve performance and code readability.
